//2.Напишите функцию getPercents(percent, number), которая возвращает {percent} процентов от {number}.
// Пояснение: например, getPercents(30, 200) должно вернуть 60.

const arrowGetPercents = (percent, number) => {
  return result = number * percent / 100
   result;
}

// let percents = arrowGetPercents(30, 200);
//
// console.log(percents)